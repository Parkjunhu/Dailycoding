11.16(화)
- 가운데 글자 가져오기(20분)
- 같은 숫자는 싫어(40분) // 시간초과
- 나누어 떨어지는 숫자 배열(15분)
- 두 정수 사이의 합(15분)

11.17(수)
- 문자열 내 p와 y의 개수(20분 소요)
- 문자열 다루기 기본(30분 소요) // 채점실패
- 서울에서 김서방 찾기(10분 소요)
- 수박수박수박수박수박수?(20분 소요)
- 문자열을 정수로 바꾸기(10분소요)

11.18(목)
- 소수 찾기(50분) // 시간초과
- 약수의 합(15분)
- 이상한 문자 만들기(50분) // 시간초과

11.19(금)
- 자릿수 더하기 (50분)
- 자연수 뒤집어 배열로 만들기(30분) // 채점실패
- 정수 제곱근 판별(30분)
